package oop;

public interface IRate {
	
	//like a contract where other classes can sign with to use this interface
	//Write the class outline - HAS a relationship with the other classes --but is not a relationship as parent
	public void setRate();
	public void increaseRate();
	
}
